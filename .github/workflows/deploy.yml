name: Deploy to Render

on:
  push:
    branches:
      - master
  workflow_dispatch: # Permet de déclencher manuellement le déploiement

jobs:
  deploy:
    runs-on: ubuntu-latest

    env:
      NODE_ENV: production # Force le mode production

    steps:
      # 1️⃣ Récupérer le code
      - name: Checkout code
        uses: actions/checkout@v3

      # 2️⃣ Installer les dépendances
      - name: Install dependencies
        run: npm ci

      # 3️⃣ Lancer les tests (optionnel)
      - name: Run tests
        run: npm test || echo "⚠️ Tests ignorés ou échoués"

      # 4️⃣ Vérifier l'environnement
      - name: Check environment
        run: |
          if [ "$NODE_ENV" != "production" ]; then
            echo "🛑 Pas en production, annulation"
            exit 1
          fi
          echo "✅ Environnement de production détecté"

      # 5️⃣ Installer le CLI Render
      - name: Install Render CLI
        run: |
          curl -L https://github.com/render-oss/cli/releases/download/v1.1.0/cli_1.1.0_linux_amd64.zip -o render.zip
          unzip render.zip
          chmod +x render
          sudo mv render /usr/local/bin/render
          render --version

      # 6️⃣ Déployer sur Render
      - name: Deploy to Render
        env:
          RENDER_API_KEY: ${{ secrets.RENDER_API_KEY }}
          CI: true
        run: |
          echo "🚀 Lancement du déploiement sur Render..."
          DEPLOY_OUTPUT=$(render deploys create ${{ secrets.RENDER_SERVICE_ID }} --confirm --output json)
          echo "$DEPLOY_OUTPUT"

          DEPLOY_URL=$(echo "$DEPLOY_OUTPUT" | grep -oP '(?<="url":")[^"]*')
          echo "✅ Déploiement lancé sur : $DEPLOY_URL"
