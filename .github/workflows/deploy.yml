name: Deploy to Render

on:
  push:
    branches:
      - master
    paths:
      - 'src/**'
      - 'package.json'
      - 'package-lock.json'

jobs:
  deploy:
    runs-on: ubuntu-latest

    env:
      NODE_ENV: production

    steps:
      # 1️⃣ Récupérer le code
      - name: Checkout code
        uses: actions/checkout@v3

      # 2️⃣ Installer les dépendances
      - name: Install dependencies
        run: npm ci

      # 3️⃣ Vérifier que c'est bien en prod
      - name: Check environment
        run: |
          if [ "$NODE_ENV" != "production" ]; then
            echo "🛑 Pas en production, annulation du déploiement"
            exit 0
          fi
          echo "✅ Environnement de production détecté, on continue..."

      # 4️⃣ Installer Render CLI sans demander confirmation
      - name: Install Render CLI
        run: |
          RENDER_VERSION="1.1.0"
          curl -L "https://github.com/render-oss/cli/releases/download/v${RENDER_VERSION}/cli_${RENDER_VERSION}_linux_amd64.zip" -o render.zip
          unzip -jo render.zip cli_v${RENDER_VERSION} -d .
          chmod +x cli_v${RENDER_VERSION}
          sudo mv cli_v${RENDER_VERSION} /usr/local/bin/render
          render --version

      # 5️⃣ Déploiement sur Render
      - name: Deploy to Render
        env:
          RENDER_API_KEY: ${{ secrets.RENDER_API_KEY }}
          CI: true
        run: |
          DEPLOY_OUTPUT=$(render deploys create ${{ secrets.RENDER_SERVICE_ID }} --confirm --output json)
          echo "$DEPLOY_OUTPUT"

          DEPLOY_URL=$(echo "$DEPLOY_OUTPUT" | grep -oP '(?<="url":")[^"]*')
          echo "🚀 Déploiement lancé sur : $DEPLOY_URL"
